{
  "name": "sinhala-design-stages",
  "private": true,
  "type": "commonjs",
  "description": "Dependency-based Sinhala font development stage model. Source of truth: sinhala-design-dependencies.yaml. Run `npm run build` to validate the model and regenerate index.html.",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "js-yaml": "^5.2.1"
  }
}
